export const changeBtn = async (isFollowing) => {
  //when clicked the btn change state that isfollowing change the targetuser
  // const Following = followingList.includes(targetUser);
  console.log(isFollowing);
  console.log('hello ')
}